// Exchange rate DB service removed as multi-currency support is no longer used.
// This file remains as a stub so accidental references fail fast.

// Exchange rate DB service removed as multi-currency support is no longer used.
// This file remains as a stub so accidental references fail fast.

class ExchangeRateDBService {
  factory ExchangeRateDBService() {
    throw UnsupportedError(
      'ExchangeRateDBService was removed with multi-currency support.',
    );
  }
}
