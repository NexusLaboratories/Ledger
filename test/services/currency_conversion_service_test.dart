// Currency conversion tests removed — feature deprecated. Placeholder test to keep CI stable.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('currency conversion tests removed', () {
    expect(1, equals(1));
  });
}
